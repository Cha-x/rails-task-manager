<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= task.completed ? "✅" : "❎" %> <%= link_to task.title, task_path(task) %>&nbsp;|&nbsp;<%= link_to "🗑", task_path(task), method: :delete, data: { confirm: "Delete #{task.title} ?" } %>  </li>
  <% end %>
</ul>

<p> <%= link_to "+ Add a new task", new_task_path(@task) %> </p>
